apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mlflow.fullname" . }}
data:
  {{- range $key, $val := .Values.envSecrets }}
  {{ $key }}: {{ $val | quote }}
  {{- end}}

  {{- range $key, $val := .Values.fileSecrets }}
  {{ $key }}: {{ $val.value  | quote }}
  {{- end}}
